# Mafia Protocol — Environment Variables
# Copy this to .env for local overrides (never commit .env)

# Server port (Render/Railway set this automatically)
PORT=8080

# Max players allowed per room
MAX_ROOM_SIZE=12

# Max websocket messages per second per client before kick
RATE_LIMIT_MSG_PER_SEC=30
